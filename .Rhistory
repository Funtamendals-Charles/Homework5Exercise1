#Create function to load dataframe from Github url.
load_data = function(url) {
tmp = tempfile(fileext = ".xlsx")
download.file(url, tmp, mode = "wb")
return (tmp)
}
data_2006 = load_data("https://github.com/Funtamendals-Charles/Homework5Exercise1/raw/refs/heads/main/Data/fsi-2006.xlsx")
#Create function to load dataframe from Github url.
load_data = function(url) {
tmp = tempfile(fileext = ".xlsx")
download.file(url, tmp, mode = "wb")
return read_excel(tmp)
#Create function to load dataframe from Github url.
load_data = function(url) {
tmp = tempfile(fileext = ".xlsx")
download.file(url, tmp, mode = "wb")
return (read_excel(tmp))
}
data_2006 = load_data("https://github.com/Funtamendals-Charles/Homework5Exercise1/raw/refs/heads/main/Data/fsi-2006.xlsx")
# Install the 'readxl' package
#install.packages("readxl")
#install.packages("sjPlot")
library(readxl)
#Create function to load dataframe from Github url.
load_data = function(url) {
tmp = tempfile(fileext = ".xlsx")
download.file(url, tmp, mode = "wb")
return (read_excel(tmp))
}
data_2006 = load_data("https://github.com/Funtamendals-Charles/Homework5Exercise1/raw/refs/heads/main/Data/fsi-2006.xlsx")
View(data_2006)
#Step 1: Create helper functions for loading data from Github
#Create function to load data into a data frame from Github URL.
#Function load_data takes in the Github url as input, temporary saves the .xlsx file and returns the data as a data frame. R needs a local path to read .xlsx files. The following function streamlines the process.
load_data = function(url) {
#We temporarily create a file path on our system, we specify that it will store a .xlsx file
tmp = tempfile(fileext = ".xlsx")
#Then we download the file from our Github URL and store it to our temp path. We specify "wb" for write and binary mode, best for Windows systems
download.file(url, tmp, mode = "wb")
#Finally we return the data frame that can now be read by read_excel.
return (read_excel(tmp))
}
#
get_link = function(year) {
link_start = "https://github.com/Funtamendals-Charles/Homework5Exercise1/raw/refs/heads/main/Data/fsi-"
link_year = str(year)
link_end = ".xlsx"
return (paste0(link_start, link_year, link_end))
}
get_link(2006)
#Step 1: Create helper functions for loading data from Github
#Create function to load data into a data frame from Github URL.
#Function load_data takes in the Github url as input, temporary saves the .xlsx file and returns the data as a data frame. R needs a local path to read .xlsx files. The following function streamlines the process.
load_data = function(url) {
#We temporarily create a file path on our system, we specify that it will store a .xlsx file
tmp = tempfile(fileext = ".xlsx")
#Then we download the file from our Github URL and store it to our temp path. We specify "wb" for write and binary mode, best for Windows systems
download.file(url, tmp, mode = "wb")
#Finally we return the data frame that can now be read by read_excel.
return (read_excel(tmp))
}
#
get_link = function(year) {
link_start = "https://github.com/Funtamendals-Charles/Homework5Exercise1/raw/refs/heads/main/Data/fsi-"
link_year = str(year)
link_end = ".xlsx"
return (paste0(link_start, link_year, link_end))
}
get_link(2006)
str(2006)
#Step 1: Create helper functions for loading data from Github
#Create function to load data into a data frame from Github URL.
#Function load_data takes in the Github url as input, temporary saves the .xlsx file and returns the data as a data frame. R needs a local path to read .xlsx files. The following function streamlines the process.
load_data = function(url) {
#We temporarily create a file path on our system, we specify that it will store a .xlsx file
tmp = tempfile(fileext = ".xlsx")
#Then we download the file from our Github URL and store it to our temp path. We specify "wb" for write and binary mode, best for Windows systems
download.file(url, tmp, mode = "wb")
#Finally we return the data frame that can now be read by read_excel.
return (read_excel(tmp))
}
#
get_link = function(year) {
link_start = "https://github.com/Funtamendals-Charles/Homework5Exercise1/raw/refs/heads/main/Data/fsi-"
link_year = str(year)
link_end = ".xlsx"
return (paste0(link_start, link_year, link_end))
}
get_link(2006)
as.character(2006)
#Step 1: Create helper functions for loading data from Github
#Create function to load data into a data frame from Github URL.
#Function load_data takes in the Github url as input, temporary saves the .xlsx file and returns the data as a data frame. R needs a local path to read .xlsx files. The following function streamlines the process.
load_data = function(url) {
#We temporarily create a file path on our system, we specify that it will store a .xlsx file
tmp = tempfile(fileext = ".xlsx")
#Then we download the file from our Github URL and store it to our temp path. We specify "wb" for write and binary mode, best for Windows systems
download.file(url, tmp, mode = "wb")
#Finally we return the data frame that can now be read by read_excel.
return (read_excel(tmp))
}
#
get_link = function(year) {
link_start = "https://github.com/Funtamendals-Charles/Homework5Exercise1/raw/refs/heads/main/Data/fsi-"
link_year = as.character(year)
link_end = ".xlsx"
return (paste0(link_start, link_year, link_end))
}
get_link(2006)
#Step 1: Create helper functions for loading data from Github
#Create function to load data into a data frame from Github URL.
#Function load_data takes in the Github url as input, temporary saves the .xlsx file and returns the data as a data frame. R needs a local path to read .xlsx files. The following function streamlines the process.
load_data = function(url) {
#We temporarily create a file path on our system, we specify that it will store a .xlsx file
tmp = tempfile(fileext = ".xlsx")
#Then we download the file from our Github URL and store it to our temp path. We specify "wb" for write and binary mode, best for Windows systems
download.file(url, tmp, mode = "wb")
#Finally we return the data frame that can now be read by read_excel.
return (read_excel(tmp))
}
#Create function to generate Github URL from year
get_link = function(year) {
link_start = "https://github.com/Funtamendals-Charles/Homework5Exercise1/raw/refs/heads/main/Data/fsi-"
link_year = as.character(year)
link_end = ".xlsx"
return (paste0(link_start, link_year, link_end))
}
#These helper functions will help to .....
data_years = seq(2006, 2023)
data_years = as.list(2006, 2023)
View(data_years)
data_years = as.list(2006:2023)
#Generate a list of years that span the data (from 2006 to 2023)
data_years_list = as.list(2006:2023)
data_URL_list = lapply(data_years_list, get_link)
View(data_URL_list)
#Generate a list of years that span the data (from 2006 to 2023)
data_years_list = as.list(2006:2023)
data_URL_list = lapply(data_years_list, get_link)
str(data_URL_list)
#Generate a list of years that span the data (from 2006 to 2023)
data_years_list = as.list(2006:2023)
data_URL_list = lapply(data_years_list, get_link)
data_frame_list = lapply(data_URL_list, load_data)
View(data_frame_list)
#Generate a list of years that span the data (from 2006 to 2023)
data_years_list = as.list(2006:2023)
data_URL_list = lapply(data_years_list, get_link)
data_frame_list = lapply(data_URL_list, load_data)
all_data = do.call(rbind, data_frame_list)
names(data_2006)
#Step 2: Load all needed data from Github
#Each variable will hold the data frame for the data of the corresponding year using the previously mentioned load_data function.
data_2006 = load_data("https://github.com/Funtamendals-Charles/Homework5Exercise1/raw/refs/heads/main/Data/fsi-2006.xlsx")
data_2019 = load_data("https://github.com/Funtamendals-Charles/Homework5Exercise1/raw/refs/heads/main/Data/fsi-2019.xlsx")
View(data_2019)
names(data_2006)
data_2019[,names(data_2006)]
#Generate a list of years that span the data (from 2006 to 2023)
data_years_list = as.list(2006:2023)
data_URL_list = lapply(data_years_list, get_link)
data_frame_list_unfiltered = lapply(data_URL_list, load_data)
relavent_columns = names(data_frame_list_unfiltered[1])
data_frame_list = lapply(data_frame_list_unfiltered, function(df) {return (df[,relavent_columns])})
all_data = do.call(rbind, data_frame_list)
View(data_frame_list)
#Generate a list of years that span the data (from 2006 to 2023)
data_years_list = as.list(2006:2023)
data_URL_list = lapply(data_years_list, get_link)
data_frame_list_unfiltered = lapply(data_URL_list, load_data)
#relavent_columns = names(data_frame_list_unfiltered[1])
#data_frame_list = lapply(data_frame_list_unfiltered, function(df) {return (df[,relavent_columns])})
#all_data = do.call(rbind, data_frame_list)
View(data_frame_list_unfiltered)
relavent_columns = names(data_frame_list_unfiltered[1])
relavent_columns = names(data_frame_list_unfiltered[[1]])
#Generate a list of years that span the data (from 2006 to 2023)
data_years_list = as.list(2006:2023)
data_URL_list = lapply(data_years_list, get_link)
data_frame_list_unfiltered = lapply(data_URL_list, load_data)
relevant_columns = names(data_frame_list_unfiltered[[1]])
data_frame_list = lapply(data_frame_list_unfiltered, function(df) {return (df[,relevent_columns])})
#Generate a list of years that span the data (from 2006 to 2023)
data_years_list = as.list(2006:2023)
data_URL_list = lapply(data_years_list, get_link)
data_frame_list_unfiltered = lapply(data_URL_list, load_data)
relevant_columns = names(data_frame_list_unfiltered[[1]])
data_frame_list = lapply(data_frame_list_unfiltered, function(df) {return (df[,relevant_columns])})
all_data = do.call(rbind, data_frame_list)
View(all_data)
View(data_frame_list_unfiltered[[1]])
View(data_frame_list_unfiltered[[15]])
View(data_frame_list_unfiltered[[17]])
View(data_frame_list_unfiltered[[16]])
View(data_frame_list_unfiltered[[15]])
View(data_frame_list_unfiltered[[16]])
relavent_columns = names(data_frame_list_unfiltered[[1]])
data_frame_list_unfiltered[[16]][,relevant_columns]
View(data_frame_list)
View(data_frame_list[[16]])
names(all_data)
str(all_data)
#Cleaning
all_data = lapply(all_data, function(df) {
df$Year = as.integer(df$Year)
return (df)
})
#Cleaning
all_data$Year = as.integer(all_data$Year)
View(data_frame_list[[1]])
View(data_frame_list[[2]])
View(data_frame_list[[8]])
View(data_frame_list[[12]])
View(data_frame_list[[18]])
View(data_frame_list[[17]])
View(data_frame_list[[13]])
#Generate a list of years that span the data (from 2006 to 2023)
data_years_list = as.list(2006:2023)
#Using get_link generate all needed Github links (for data from 2006 to 2023)
data_URL_list = lapply(data_years_list, get_link)
#Using load_data load all data into a list of data frames,
data_frame_list_unfiltered = lapply(data_URL_list, load_data)
relevant_columns = names(data_frame_list_unfiltered[[1]])
data_frame_list = lapply(data_frame_list_unfiltered, function(df) {return (df[,relevant_columns])})
all_data = do.call(rbind, data_frame_list)
#Cleaning
as.integer(format(all_data$year, "%Y"))
#Cleaning
as.integer(format(all_data$Year, "%Y"))
#Cleaning
all_data$Year = as.integer(format(all_data$Year, "%Y"))
#Cleaning
all_data$Year = as.integer(format(all_data$Year, "%Y"))
as.integer(format(all_data$Year, "%Y"))
#Generate a list of years that span the data (from 2006 to 2023)
data_years_list = as.list(2006:2023)
#Using get_link generate all needed Github links (for data from 2006 to 2023)
data_URL_list = lapply(data_years_list, get_link)
#Using load_data load all data into a list of data frames,
data_frame_list_unfiltered = lapply(data_URL_list, load_data)
relevant_columns = names(data_frame_list_unfiltered[[1]])
data_frame_list = lapply(data_frame_list_unfiltered, function(df) {return (df[,relevant_columns])})
all_data = do.call(rbind, data_frame_list)
all_data_clean = all_data
#Cleaning
all_data_clean$Year = as.integer(format(all_data$Year, "%Y"))
str(all_data_clean)
#Cleaning
all_data_clean$Year = as.integer(format(all_data$Year, "%Y"))
str(all_data_clean)
